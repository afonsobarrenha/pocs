# Node-js

Comandos comuns

```
node app.js #chamada "pura"
npm init
npm install express --save
npm install ejs --save
sudo npm install -g nodemon #não dá o --save pois é para dev
nodemon app #chamada através do nodemon
npm install mysql --save
```

